# sqlite database and its WAL/SHM sidecars
caravan.db*

# clean-shutdown marker (SPEC §2.3) — per-install runtime state
caravan.state

# built binary (the module is also named caravan, so anchor to the repo root)
/caravan

# Air development artifacts
/tmp/

# web toolchain output
web/node_modules/
web/dist/

# Compose/runtime directories and local environment files
/config/
/data/
.env
.env.*
**/.env
**/.env.*

# macOS
.DS_Store

# Agents
.claude
.agents
.omp
.codex

/plans/